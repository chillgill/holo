!param
id: after
name: "$filter"
name: "$filter"
desc: "Filter out the item type using the 'isOf' option. Only the 'isOf' option is supported for item type checking."
schema: 
    type: string
    examples: 
        - 
            example: "$filter=isof('BusinessRule')"
        - 
            example: "$filter=isof('TextNote')"
